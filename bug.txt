- Clean/refactor the code
- Add Ringplus icons
- Make sure app works on IOS the same way as the Android one
- Check Voicemail From_number against contacts if found show Contacts name instead of from_name
- When an Incoming was missed. Need to show missed call notification in notifications
- When accepting a call when the app is not running (following a push notification), we need skip Androind splash screen to in_call_screen
- Automatic/Force update of the app
- Order Recents by DESC
- When changing Account Recents doesnt get clean up

SDK Siprix:
https://pub.dev/documentation/siprix_voip_sdk/latest/index.html
https://docs.siprix-voip.com/
https://github.com/siprix/FlutterPluginFederated/tree/main

---

On Foreground: 
    - Incoming call, phone is not ringing or vibrating (we have the incoming call screen). You need to use callkit for it
IOS:
    Incoming Calls:
        Unlocked and app terminated: 
            - Accept on Callkit accept button: OK 
            - Accept by tapping notification bar: OK 
            - Reject on Callkit button: OK
            - Reject by tapping notification bar: OK
            - Call and cancel: OK
        
        On the keypad screen, we need space between numeric keys
        Call history is not working on IOS
        in Call history new calls is showing at the bottom, we should show newest on the top
        On Reject caller app crashes
        Online/Offline is not updating dynamically (when network condition change, on registration fails...)


        I have a case: I open the app and it's not registering but it shows as online.




